[package]
name = "winisland-core"
version.workspace = true
edition = "2024"
publish = false

[dependencies]
log = { version = "0.4", features = ["std"] }
serde = { version = "1.0", features = ["derive"] }
toml = "1.1.2"
